import React from "react";

const NavCategoryTitle = ({ title }) => {
  return (
    <div>
      <h2 className="pt-8 pb-4 text-2xl font-bold text-white">{title}</h2>
    </div>
  );
};

export default NavCategoryTitle;
